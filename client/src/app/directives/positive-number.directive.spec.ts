import { PositiveNumberDirective } from "./positive-number.directive";

describe("PostiveNumberDirective", () => {
  it("should create an instance", () => {
    const directive = new PositiveNumberDirective();
    expect(directive).toBeTruthy();
  });
});
